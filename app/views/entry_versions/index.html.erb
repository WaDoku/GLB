<h1>Versions Index </h1>
<table class="table">
  <tr>
    <th>Version</th>
    <th>Datum</th>
    <th>Editiert von:</th>
    <th>Aktion</th>
  </tr>

  <% @versions.each_with_index do |version, index| %>
    <tr>
      <td><%= index == 0 ? 'Aktuelle Version' : index %></td>
      <td><%= "#{version.created_at.strftime('%b %d, %Y')}" %></td>
      <td><%= "#{version.user_name}" %></td>
      <td><%= link_to "Anzeigen", index == 0 ? entry_path(@entry) : entry_version_path(version.item_id, version.id, {version_number: index}) %></td>
    </tr>
  <% end %>
</table>
